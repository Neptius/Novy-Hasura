- table:
    schema: public
    name: auth_provider
  object_relationships:
  - name: auth_provider_config
    using:
      manual_configuration:
        remote_table:
          schema: public
          name: auth_provider_config
        column_mapping:
          id: auth_provider_id
- table:
    schema: public
    name: auth_provider_config
