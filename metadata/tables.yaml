- table:
    schema: public
    name: auth_provider
  object_relationships:
  - name: auth_provider_config
    using:
      manual_configuration:
        remote_table:
          schema: public
          name: auth_provider_config
        column_mapping:
          auth_provider_config_id: id
- table:
    schema: public
    name: auth_provider_config
